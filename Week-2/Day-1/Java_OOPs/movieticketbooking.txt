class Ticket:
    movieName: String
    seatNo: String
    price: Double
    status: String ("Booked"/"Cancelled")

class Theatre:
    movies: List<String>
    availableSeats: Map<Movie, List<String>>
    
    method bookTicket(customer, movie, seat):
        if seat available:
            create new Ticket(movie, seat, price, "Booked")
            update seat map
            return Ticket
        else:
            return "Seat not available"

    method cancelTicket(ticket):
        ticket.status = "Cancelled"
        update seat map

class Customer:
    name: String
    method bookMovie(theatre, movie, seat)
    method cancelMovie(theatre, ticket)

Main:
    theatre = new Theatre()
    customer = new Customer("Ravi")

    ticket = customer.bookMovie(theatre, "Avengers: Endgame", "A12")
    print(ticket.status)

    customer.cancelMovie(theatre, ticket)
    print(ticket.status)
